import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // hide the floating Next.js dev badge in the bottom-left of the viewport
  devIndicators: false,
};

export default nextConfig;
